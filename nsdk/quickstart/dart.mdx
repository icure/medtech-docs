# Dart

To include the SDK in your dart project, you just need to add the dependency to your pubspec.yaml configuration.

```bash
flutter pub add cardinal_sdk
```

:::note
The Cardinal Dart SDK currently supports only the Android and iOS platforms.
:::